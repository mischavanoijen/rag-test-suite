# CrewAI Test Suite Environment Variables
# Copy this file to .env and fill in your values

# ═══════════════════════════════════════════════════════════════════════════════
# LLM CONFIGURATION (Required for all modes)
# ═══════════════════════════════════════════════════════════════════════════════
# Via Konecta LiteLLM Proxy
OPENAI_API_KEY=sk-your-litellm-proxy-key
OPENAI_API_BASE=https://litellm-proxy-xxx.run.app/v1

# ═══════════════════════════════════════════════════════════════════════════════
# RAG BACKEND (Required for discovery/test generation modes)
# Choose Option A or Option B based on your RAG infrastructure
# ═══════════════════════════════════════════════════════════════════════════════

# Option A: RAG Engine (MCP SSE Transport)
# Note: Do NOT include /mcp or /sse suffix - the tool adds /sse automatically
PG_RAG_MCP_URL=https://your-rag-mcp-server.run.app
PG_RAG_TOKEN=your-rag-mcp-token
PG_RAG_CORPUS=projects/xxx/locations/region/ragCorpora/yyy

# Option B: Qdrant Vector Database
QDRANT_URL=https://your-qdrant-instance.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key
QDRANT_COLLECTION=your-collection-name

# ═══════════════════════════════════════════════════════════════════════════════
# TARGET CREW (Required for execution modes: full, execute_only)
# ═══════════════════════════════════════════════════════════════════════════════

# API Mode: For testing deployed crews via CrewAI Enterprise
TARGET_API_URL=https://app.crewai.com/api/v1/crews/{crew_id}/kickoff
TARGET_API_TOKEN=your-crewai-api-token

# Local Mode: For testing crews locally (set these if using mode=local)
# TARGET_CREW_PATH=/path/to/your/crew
# TARGET_CREW_MODULE=your_crew.main

# ═══════════════════════════════════════════════════════════════════════════════
# RUN MODE OPTIONS
# ═══════════════════════════════════════════════════════════════════════════════
# Set via CLI --run-mode or RUN_MODE env var:
#   full             - Discovery → Prompts → Tests → Execute → Report
#   prompt_only      - Discovery → Prompt suggestions only
#   generate_only    - Discovery → Prompts → Test cases (no execution)
#   execute_only     - Load tests from CSV → Execute → Report
#   generate_and_execute - Same as full (default)

# RUN_MODE=full
# NUM_TESTS=20
# CREW_DESCRIPTION=Customer support assistant for retail queries
